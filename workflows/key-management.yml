
name: 🔑 Gestión de Claves GPG desde Issues

on:
  issue_comment:
    types: [created]

jobs:
  process-key:
    if: contains(github.event.comment.body, '🔐 clave-verificada')
    runs-on: ubuntu-latest
    steps:
    - name: Clonar repositorio
      uses: actions/checkout@v3

    - name: Añadir clave a rfc/KEYS.txt (simulado)
      run: |
        echo "➡️ Este flujo simula la inclusión de clave si estuviera definida como dato estructurado."
